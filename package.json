{
  "name": "bridge-token",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ sunguru98/bridge-token",
    "create-local": "graph create --node http://localhost:8020/ sunguru98/bridge-token",
    "remove-local": "graph remove --node http://localhost:8020/ sunguru98/bridge-token",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sunguru98/bridge-token"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
